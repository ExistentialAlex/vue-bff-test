{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "format": "lerna run format"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.2"
  }
}
